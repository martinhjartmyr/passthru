## Agent skills

### Issue tracker

Issues live as GitHub issues (uses `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` at the repo root, `docs/adr/` for decisions. See `docs/agents/domain.md`.
